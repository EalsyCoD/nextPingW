export interface GamesActionProps {
  title: string;
  id: number;
  producer: string;
  uri: string;
}

export interface GameData {
  favorite: boolean;
  id: string;
  producer: string;
  url: string;
}
